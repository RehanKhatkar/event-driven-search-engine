global:
  scrape_interval: 5s
scrape_configs:
  - job_name: 'spring-boot-backends'
    metrics_path: '/actuator/prometheus'
    dns_sd_configs:
      - names:
          - 'backend-app'
        type: 'A'
        port: 8080
